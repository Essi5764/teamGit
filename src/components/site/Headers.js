import {
    Navbar,
    NavItem,
} from 'reactstrap';


const Header = () => {
    return(
        <header>
            <h1 className='header'>
                What's happening in:</h1>
            
        </header>
    );
};

export default Header;

